# https://www.terraform.io/docs/configuration/providers.html#provider-plugin-cache
# plugin_cache_dir = "{{ R_terraform_dir.path }}/plugin-cache"

{% for domain, token in tf_domains_to_tokens.items() %}
credentials "{{ domain }}" {
  token = "{{ token }}"
}

{% endfor %}
