#!/usr/bin/env sh

# cannot hash a whole directory with templates. instead keep a manual list.
# fonts: consolas,hack,mono

cp {{ .chezmoi.sourceDir }}/fonts/**/*.TTF ~/Library/Fonts
chmod 0600 ~/Library/Fonts/*.TTF
