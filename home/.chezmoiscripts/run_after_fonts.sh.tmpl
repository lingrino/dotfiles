#!/usr/bin/env sh
set -euo pipefail

# cannot hash a whole directory with templates. instead keep a manual list.
# fonts: consolas,hack,mono,usgraphics

sudo cp {{ .chezmoi.sourceDir }}/fonts/**/*.TTF {{ .chezmoi.homeDir }}/Library/Fonts
sudo chmod 0644 {{ .chezmoi.homeDir }}/Library/Fonts/*.TTF
